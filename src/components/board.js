import React from "react"
import styled from "styled-components"

const StyledBoard = styled.div`
  width: 100%;
  height: 50vh;
  background: white;
  border-radius: 4px;
  box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.2);
  margin-top: 100px;
`

const Board = () => {
  return <StyledBoard>asdasd</StyledBoard>
}

export default Board
